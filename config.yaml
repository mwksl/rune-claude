# Rune package overlay: `pkg install github.com/mwksl/rune-claude`
# installs the claude-changes extension, run from source via the Go toolchain.
requirements:
  - go
extensions:
  claude-changes:
    path: '$RUNE_DATADIR/lib/$RUNE_PKG_ID'
